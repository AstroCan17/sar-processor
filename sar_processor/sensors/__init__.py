"""Sensor adaptation layer (C-SENSORS): per-sensor SAR profile loading.

Holds no instrument constants or algorithms in code; calibration data lives in
ADFs referenced by URI (trace REQ-AD-*). First instantiated profile: a C-band
spaceborne SAR (Sentinel-1 C-SAR as the reference public dataset).
"""
