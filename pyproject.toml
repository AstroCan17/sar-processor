# Copyright 2026 ESA
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#   http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "sar_processor"
authors = [{name = "AstroCan17"}]
dynamic = ["version", "description"]
readme = "README.md"
classifiers = [
    "License :: OSI Approved :: Apache Software License",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: Implementation :: CPython",
    "Topic :: Scientific/Engineering",
    "Topic :: Software Development",
    "Typing :: Typed",
    "Private :: Do Not Upload",
]
requires-python = ">=3.11"

# EOPF CPM is pinned to a fixed version for a stable build environment,
# matching the version available in the SDE cpm-build-environment image
# (same pin as the sibling ipf/msi-processor project on this SDE).
#
# SAR-specific numerical dependencies (e.g. FFT backends for range/azimuth
# compression) are deliberately NOT declared yet: the processing-stage set is
# baselined at PDR (SDD), and eopf already provides numpy/dask/zarr. Additions
# must be justified in the SDD and recorded here with a rationale comment.
dependencies = [
    "eopf == 2.8.1",
]
license = {file = "LICENSE"}

[project.optional-dependencies]
cluster-plugin = ["eopf[cluster-plugin] >= 1.5.0"]
tests = ["eopf[tests] >= 1.5.0"]
linter = ["eopf[linter] >= 1.5.0"]
typing = ["eopf[typing] >= 1.5.0"]
formatter = ["eopf[formatter] >= 1.5.0"]
security = ["eopf[security] >= 1.5.0"]
notebook = ["eopf[notebook] >= 1.5.0"]
doc = [
    "eopf[doc] >= 1.5.0",
    "sphinxcontrib-mermaid",
]
complexity = ["eopf[complexity] >= 1.5.0"]
doc-cov = ["eopf[doc-cov] >= 1.5.0"]
dev = ["eopf[dev] >= 1.5.0"]

[tool.isort]
profile = "black"

[tool.black]
line-length = 120
target-version = ['py311']

[tool.pytest.ini_options]
markers = [
    "integration",
    "unit",
    "need_files",
]
